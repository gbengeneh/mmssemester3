apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: semester4app
  labels:
    app: postgres
spec:
  clusterIP: None  # Headless for StatefulSet
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres
---

