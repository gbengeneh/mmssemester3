apiVersion: v1
kind: ConfigMap
metadata:
  name: microservices-config
  namespace: semester4app
data:
  # No eureka/config as per request - services use Spring Cloud LoadBalancer
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: semester4app
data:
  POSTGRES_DB: "user_service"
  PGDATA: "/var/lib/postgresql/data/pgdata"

